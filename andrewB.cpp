//Andrew Bainbridge
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <time.h>
#include <math.h>
#include <X11/Xlib.h>
#include <X11/keysym.h>
#include <GL/glx.h>
#include "fonts.h"




bool OnGround = true;

	void JumpUp()
{}
/*
{
    if (OnGround) {
    render->cy += 11.0f;
    OnGround = false;
    }
    else {
    return; //char in air
    }
}
	void UpdatePos()
{
    velY += gravity;
    posY += velY;
    posX += velX;
    if (posY > ) {
    posY =  ;
    velY = 0.0;
    OnGround = true;
    }

}
*/
